Todo:
-O2 -> -O
path will happily return an invalid path if no wildcards
don't need "err = " in recursive matchPath() call
PLOAD command
Switch to window mode and create window on fly if in console mode
Save/load window picture 

Rainy day:
number formatting
print procs to printer
Text in graphics window
fmt with no int or float part
